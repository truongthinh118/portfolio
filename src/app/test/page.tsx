import ProjectSlider from "@/components/ProjectSlider";

export default function Page() {
  return (
    <main className="flex size-full min-h-[90vh] items-center justify-center bg-default">
      <ProjectSlider />
    </main>
  );
}
